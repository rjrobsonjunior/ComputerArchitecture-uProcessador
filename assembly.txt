(A) carrega na RAM os valores 1 a 1024

ld r1, 1
movA r1
addi 1
movR r1
lw r1
cmp 1024
jz -5

(B) para todos os valores da RAM, zero todos multiplos do mesmo
para _i ← 1024 = i
  imprima “O número ” i ” é primo.”
  para j 
←
← i+i até valorLimite, de i e i
   vetor[j] 
←
← 0
  fim-para
 fim-se
fim-para

ld r1, 2
movA r1
add r1
cmp 32

